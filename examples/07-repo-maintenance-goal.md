# Example 07: Repo-Maintenance Goal

## Input
## Better Prompt
## Why It Works

- It separates triage (read-only) from execution, so nothing is deleted before a human sees the list.
- It bounds "clean up" to a small, reviewable batch instead of an open-ended rewrite.
- It produces a closeout checklist, so the result is auditable instead of a silent diff.
